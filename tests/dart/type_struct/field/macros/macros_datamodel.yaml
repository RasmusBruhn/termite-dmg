namespace:
- test
macros:
  MACRO: "1"
data_types:
  DataType:
    data: !Struct
      fields:
      - name: field1
        data_type: integer
        default: !Default "$MACRO$"
      - name: field2
        data_type: number
        default: !Optional
